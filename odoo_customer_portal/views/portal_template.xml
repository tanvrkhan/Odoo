<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="portal_my_home_attachments" name="Show Attachments" customize_show="True"
              inherit_id="portal.portal_my_home" priority="20">
        <xpath expr="//div[hasclass('o_portal_docs')]" position="inside">
            <t t-call="portal.portal_docs_entry">
                <t t-set="title">Attachments</t>
                <t t-set="url" t-value="'/add/attachments'"/>
                <t t-set="placeholder_count" t-value="'attachment_count'"/>
            </t>
        </xpath>
    </template>
    <template id="portal_my_attachments" name="My Attachments">
        <t t-call="portal.portal_layout">
            <t t-set="breadcrumbs_searchbar" t-value="True"/>

            <t t-call="portal.portal_searchbar">
                <t t-set="title">Attachments</t>
            </t>
            <t t-if="not invoices">
                <p>There are currently no attachments for your account.</p>
            </t>
            <t t-if="invoices" t-call="portal.portal_table">
                <thead>
                    <tr class="active">
                        <th>Name</th>
                        <th>Expiry</th>
                        <th class="text-end">File</th>
                    </tr>
                </thead>
                <tbody>
                    <t t-foreach="invoices" t-as="invoice">
                        <tr>
                            <td>
                                <!--                                 <a t-attf-href="/my/attachment/#{invoice.id}?{{ keep_query() }}">-->
                                <t t-esc="invoice.name"/>
                                <!--                                </a>-->
                            </td>
                            <td>
                                <span t-field="invoice.expiry_date"/>
                            </td>
                            <td>
                                <span t-field="invoice.datas"/>
                            </td>


                        </tr>
                    </t>
                </tbody>
            </t>
        </t>
    </template>
    <template id="add_attachment_form" name="Project Detail">
        <t t-call="website.layout">
            <div class="col-md-offset-5 col-sm-offset-4 col-sm-8 col-md-7" style="margin-top:30px;">
                <form action="/attachment/uploaded" method="post"
                      class="s_website_form form-horizontal container-fluid mt32" enctype="multipart/form-data"
                      id="file_upload_form">
                    <div t-attf-class="form-group">
                        <div class="col-md-7 col-sm-8">
                            <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                            <!--                            <input type="hidden" name="project_id" t-att-value="project.id"/>-->
                            <label for="attachment">Attachment:</label>
                            <br/>
                            <input type="file"  name="attachment" class="file" multiple="true" data-show-upload="true"
                                   data-show-caption="true" lass="file" data-show-preview="true" id="attachment"/>
                            <br/>
                            <label for="expiry_date">Expiry Date:</label>
                            <br/>
                            <input type="date"  name="expiry_date"  id="expiry_date"/>
                            <br/>

                            <button type="submit" name="Upload" style="margin-top:20px;" class="fa fa-upload">
                                Upload
                            </button>
                        </div>
                    </div>
                </form>
            </div>
        </t>
    </template>
</odoo>
