<?xml version="1.0" encoding="utf-8" ?>
<odoo>

    <record id="view_account_move_form_inherit" model="ir.ui.view">
        <field name="name">account.move.form.inherit</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='journal_id']" position="after">
<!--                <field name="estimate_id" attrs="{'invisible': [('move_type', 'not in', ['entry'])]}"/>-->
<!--                <field name="estimate_cost_line_id" attrs="{'invisible': [('move_type', 'not in', ['entry'])]}"/>-->
                <field name="estimate_id" invisible="1"/>
                <field name="estimate_cost_line_id" invisible="1"/>
                <field name="estimate_purchase_line_id" invisible="1"/>
                <field name="estimate_sale_line_id" invisible="1"/>
            </xpath>
        </field>
    </record>

</odoo>