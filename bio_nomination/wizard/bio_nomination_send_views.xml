<?xml version="1.0" encoding="utf-8"?>
<odoo>

        <record id="bio_nomination_send_wizard_form" model="ir.ui.view">
            <field name="name">bio.nomination.send.form</field>
            <field name="model">bio.nomination.send</field>
            <field name="arch" type="xml">
                <form>
                    <field name="composition_mode" invisible="1"/>
                    <field name="nomination_ids" invisible="1"/>
                    <field name="email_from" invisible="1" />
                    <field name="mail_server_id" invisible="1"/>
                    
                    <div name="mail_form">
                        <!-- visible wizard -->
                        <div attrs="{'invisible': [('composition_mode', '=', 'mass_mail')]}">
                            <group>
                                <label for="partner_ids" string="Recipients" groups="base.group_user"/>
                                <div groups="base.group_user">
                                    <span attrs="{'invisible': [('composition_mode', '!=', 'mass_mail')]}">
                                        <strong>Email mass mailing</strong> on
                                        <span>the selected records</span>
                                    </span>
                                    <span>Followers of the document and</span>
                                    <field name="partner_ids" widget="many2many_tags_email" placeholder="Add contacts to notify..."
                                        context="{'force_email':True, 'show_email':True}" attrs="{'invisible': [('composition_mode', '=', 'mass_mail')]}"/>
                                </div>
                                <field name="subject" placeholder="Subject..." attrs="{'required': [('composition_mode', '=', 'comment')]}"/>
                            </group>
                            <field name="body" class="oe-bordered-editor" options="{'style-inline': true}"/>
                        </div>
                        <group>
                            <group attrs="{'invisible': [('composition_mode', '=', 'mass_mail')]}">
                                <field name="attachment_ids" widget="many2many_binary" string="Attach a file" nolabel="1" colspan="2" attrs="{'invisible': [('composition_mode', '=', 'mass_mail')]}"/>
                            </group>
                            <group>
                                <field name="template_id" options="{'no_create': True, 'no_edit': True}"
                                    context="{'default_model': 'bio.nomination'}"/>
                            </group>
                        </group>
                    </div>

                    <footer>
                        <button string="Send" data-hotkey="q" name="send_action" type="object" class="print btn-primary o_mail_send"/>
                        <button string="Cancel" class="btn-secondary" special="cancel" data-hotkey="z" />
                        <button icon="fa-lg fa-save" type="object" name="save_as_template" string="Save as new template"
                            attrs="{'invisible': [('composition_mode', '=', 'mass_mail')]}"
                            class="pull-right btn-secondary" help="Save as a new template" data-hotkey="w" />
                    </footer>
                </form>
            </field>
        </record>

        <record id="bio_nomination_send" model="ir.actions.server">
            <field name="name">Send</field>
            <field name="state">code</field>
            <field name="type">ir.actions.server</field>
            <field name="model_id" ref="model_bio_nomination"/>
            <field name="binding_model_id" ref="model_bio_nomination"/>
            <field name="binding_view_types">list</field>
            <field name="code">
                if records:
                    action = records.action_send()
            </field>
        </record>

</odoo>

